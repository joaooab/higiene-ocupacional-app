<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />

    <style name="TextAppearance.Toolbar.Text" parent="TextAppearance.MaterialComponents.Headline5">
        <item name="android:textColor">?attr/colorOnSurface</item>
    </style>

    <style name="CancelButton">
        <item name="android:textColor">@color/colorDisableText</item>
        <item name="android:gravity">center</item>
        <item name="android:backgroundTint">@color/colorDisable</item>
    </style>

    <style name="PrimaryButton">
        <item name="android:textAppearance">@style/TextMediumBold</item>
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:gravity">center</item>
        <item name="cornerRadius">100dp</item>
        <item name="android:radius">100dp</item>
        <item name="progressColor">@color/colorAccent</item>
        <item name="android:minHeight">47dp</item>
        <item name="android:elevation">3dp</item>
        <item name="android:background">@drawable/primary_button</item>
    </style>

    <style name="SecondaryButton" parent="PrimaryButton">
        <item name="android:textColor">@color/colorDarkBlue</item>
        <item name="android:background">@drawable/secondary_button</item>
        <item name="progressColor">@color/colorWhite</item>
    </style>

</resources>
